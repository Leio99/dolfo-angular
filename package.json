{
    "version": "1.0.0",
    "scripts": {
        "build-library": "ng build DolfoAngular",
        "new-version": "cd projects/dolfo-angular && npm run update-version && cd ../.. && npm run build-library && npm run publish-library",
        "publish-library": "npm publish ./dist"
    },
    "devDependencies": {
        "@angular-devkit/build-angular": "^20.3.4",
        "@angular/build": "^20.3.4",
        "@angular/cli": "^20.3.4",
        "ng-packagr": "^20.3.0"
    }
}
